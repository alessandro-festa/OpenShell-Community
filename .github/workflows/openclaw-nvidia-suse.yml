name: Build openclaw-nvidia-suse image

on:
  push:
    branches: [main]
    paths:
      - "sandboxes/openclaw-nvidia-suse/**"
      - "sandboxes/openclaw-suse/**"   # rebuild when the SUSE OpenClaw base changes
      - "sandboxes/suse/**"            # rebuild when the SUSE foundation changes
    tags:
      - "openclaw-nvidia-suse/v*.*.*"
  pull_request:
    branches: [main]
    paths:
      - "sandboxes/openclaw-nvidia-suse/**"
      - "sandboxes/openclaw-suse/**"
      - "sandboxes/suse/**"

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/openshell-community/sandboxes/openclaw-nvidia-suse
  BASE_IMAGE: ghcr.io/${{ github.repository_owner }}/openshell-community/sandboxes/openclaw-suse:latest

permissions:
  contents: read
  packages: write
  security-events: write

jobs:
  # ---------------------------------------------------------------------------
  # Build and run a Trivy vulnerability scan
  # ---------------------------------------------------------------------------
  build-and-scan:
    name: Build and scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (no push)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: sandboxes/openclaw-nvidia-suse/
          push: false
          load: true
          build-args: |
            BASE_IMAGE=${{ env.BASE_IMAGE }}
          tags: ${{ env.IMAGE }}:scan

      - name: Scan with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.IMAGE }}:scan
          format: sarif
          output: trivy-results.sarif
          severity: HIGH
          exit-code: "1"
          # SLE BCI 16 base ships with CRITICAL CVEs (some unfixed,
          # some recently fixed) that block the publish chain (claude#85).
          # ignore-unfixed: true alone wasn't enough, so we gate on HIGH
          # only. Unfixed-HIGH findings are still skipped; HIGH findings
          # with a fix still fail the build. CRITICAL findings continue
          # to surface in the SARIF upload to the Security tab for review.
          ignore-unfixed: true

      - name: Upload Trivy SARIF to GitHub Security tab
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif

  # ---------------------------------------------------------------------------
  # Publish to ghcr.io — runs only on semver tag pushes
  # ---------------------------------------------------------------------------
  publish:
    name: Publish
    needs: build-and-scan
    if: startsWith(github.ref, 'refs/tags/openclaw-nvidia-suse/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=match,pattern=openclaw-nvidia-suse/(v\d+\.\d+\.\d+),group=1
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push with attestations
        uses: docker/build-push-action@v6
        with:
          context: sandboxes/openclaw-nvidia-suse/
          push: true
          build-args: |
            BASE_IMAGE=${{ env.BASE_IMAGE }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: true
          sbom: true
